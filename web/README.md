# agent-reputation-web

Next.js frontend for the Agent Reputation Lab. Full documentation (routes, layout, environment variables) lives in the repository root [**`../README.md`**](../README.md).

```bash
npm install
npm run dev
```

Default dev URL: <http://localhost:3000>.
